# init() is a special method which is first run as soonas the object is created 
# init() method is also known as constructor
# it takes self argument and can also take further arguments

#Eg
class Employee:
    def __init__(self, name):
        self.name=name
        def getSalary(self):
            ...
bhushan = Employee("Bhushan")

            